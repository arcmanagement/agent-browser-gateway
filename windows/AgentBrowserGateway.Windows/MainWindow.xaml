<Window
    x:Class="AgentBrowserGateway.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AgentBrowserGateway.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Agent Browser Gateway">

    <Grid Margin="20" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock Text="Agent Browser Gateway" FontSize="24" FontWeight="SemiBold" />
            <TextBlock x:Name="StatusText" Text="Starting..." Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock x:Name="ExtensionText" Text="Extensions: 0" />
                <TextBlock x:Name="PathText" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <Button Grid.Column="1" Content="Open audit log" Click="OpenAuditLog_Click" />
            <Button Grid.Column="2" Content="Refresh" Click="Refresh_Click" />
        </Grid>

        <ListView Grid.Row="2" x:Name="TabsList">
            <ListView.Header>
                <TextBlock Text="Shared tabs" FontWeight="SemiBold" />
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnSpacing="12" Margin="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Ref}" FontFamily="Consolas" VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1" Spacing="2">
                            <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding Url}" TextTrimming="CharacterEllipsis" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                        </StackPanel>
                        <Button Grid.Column="2" Content="Revoke" Click="Revoke_Click" Tag="{Binding TabId}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Border Grid.Row="3" Padding="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="6">
            <StackPanel Spacing="6">
                <TextBlock Text="CLI setup" FontWeight="SemiBold" />
                <TextBlock Text="Place this folder on PATH, then run: abg status" TextWrapping="Wrap" />
                <TextBlock Text="Install agent skills with: npx skills add arcmanagement/agent-browser-gateway -g" FontFamily="Consolas" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
