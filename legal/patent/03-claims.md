# 特許請求の範囲

【書類名】特許請求の範囲

【請求項1】

ブラウザと、一又は複数の AI エージェントとを連携させるブラウザＡＩ連携システムであって、

前記ブラウザに付加され、前記ブラウザ内で動作し、又は前記ブラウザ若しくはブラウザ相当環境の表示画面若しくは操作対象と連携するブラウザ操作連携機能と、

前記ブラウザ操作連携機能との間で通信路又は同一実行環境内の呼出しを介して連携し、前記一又は複数の AI エージェントからの要求と前記ブラウザ操作連携機能との間を中継する Gateway モジュールと、

を備え、

前記 Gateway モジュールは、前記ブラウザ操作連携機能と前記一又は複数の AI エージェントとの間で、要求の中継、対象確認、共有許可管理、変換、監査記録、又はこれらに相当する処理の少なくとも一つを行う仲介機能であり、

前記ブラウザ、前記一又は複数の AI エージェント、及び前記 Gateway モジュールは、同一の情報処理装置上に配置されてもよく、互いに通信可能な複数の情報処理装置上に分散配置されてもよく、前記 Gateway モジュールは、前記ブラウザ内、前記ブラウザのアドオン機構内、オペレーティングシステム内、AI エージェント実行環境内、ネイティブアプリケーション内、サーバー上、クラウド環境上、又はこれらの組み合わせとして実装されてもよく、

前記 Gateway モジュールは、特定の AI サービス、AI プロバイダ、AI モデル、エージェント実行環境、クライアントアプリケーション又はツールプロトコルに依存しない呼出し形式により、前記一又は複数の AI エージェントから利用可能であり、

前記ブラウザ操作連携機能及び前記 Gateway モジュールは、特定のブラウザ自動化フレームワーク又は専用の自動化用ブラウザ環境を必須とせず、前記ブラウザを前記一又は複数の AI エージェントと連携可能にする、

ことを特徴とするシステム。

【請求項2】

請求項1に記載のシステムにおいて、

前記ブラウザ操作連携機能は、ユーザーが特定のタブ、ウィンドウ、表示領域又は操作対象について明示的な共有許可操作を行った場合に限り、当該特定のタブ、ウィンドウ、表示領域又は操作対象を共有許可済み対象として前記 Gateway モジュールに登録し、

前記 Gateway モジュールは、前記一又は複数の AI エージェントから受信した要求が前記共有許可済み対象を対象とする場合に限り、当該要求を前記ブラウザ操作連携機能に転送し、

前記ブラウザ操作連携機能は、前記共有許可済み対象の現在の取得対象識別情報が、前記明示的な共有許可操作の時点で記録された許可時取得対象識別情報と対応しない状態になったこと、前記共有許可済み対象が閉じられたこと、終了したこと、若しくは利用不能となったこと、又はユーザーによる明示的な取消操作を検知した場合に、当該共有許可済み対象の共有許可を失効させ、

前記ブラウザ操作連携機能は、前記ブラウザが提供する権限機構又はこれに相当するブラウザ連携権限機構において、全て又は不特定多数のページを対象とする恒久的又は包括的なアクセス権限を要求せず、ユーザー操作に基づき前記共有許可済み対象に限定された一時的又は範囲限定のアクセス権限により、前記共有許可済み対象に対するアクセスを取得する、

ことを特徴とするシステム。

【請求項3】

請求項2に記載のシステムにおいて、

前記ブラウザ操作連携機能は、読み取り操作については、対象が前記共有許可済み対象であることを条件として実行し、

前記共有許可済み対象の表示内容、入力状態、遷移状態又は外部入出力状態を変更し得る書き込み操作については、当該書き込み操作の内容をユーザーに提示し、ユーザーの承認を受けた場合に限り実行する、

ことを特徴とするシステム。

【請求項4】

請求項1に記載のシステムにおいて、

前記通信路は、有線接続、USB 接続、近距離無線接続、Bluetooth 接続、ローカルネットワーク、私設ネットワーク、仮想私設ネットワーク、インターネット、又はこれらを組み合わせた通信路の少なくとも一つを含み、

前記 Gateway モジュールが前記ブラウザと同一の情報処理装置上で動作する場合に、前記 Gateway モジュールは、前記ブラウザ操作連携機能との通信の待受けを同一装置内通信路又はローカル通信インターフェースに限定し、前記ブラウザ操作連携機能からの接続要求に含まれる発信元識別情報又は認証情報が許可されたブラウザ連携元を示すことを検証し、許可されない発信元からの接続要求を拒否し、

前記 Gateway モジュールは、共有許可の付与、共有許可の失効、読み取り操作、注釈操作、及び書き込み操作を含む操作履歴と、前記ブラウザ操作連携機能から受信したブラウザ内容、前記 Gateway モジュールによる変換後内容、前記一又は複数の AI エージェントから受信した操作要求若しくは注釈要求、前記ブラウザ操作連携機能へ転送した操作コマンド、又は前記一又は複数の AI エージェントへ返送した応答内容の少なくとも一部若しくは要約情報とを、前記ブラウザ、前記一又は複数の AI エージェント、若しくは前記 Gateway モジュールのいずれかが配置された情報処理装置、又はこれらと通信可能な記憶装置上の監査記録として記録する監査記録部を備え、

前記監査記録部又は前記 Gateway モジュールは、前記ブラウザ内容、前記変換後内容、前記操作要求、前記操作コマンド、又は前記応答内容が、前記一又は複数の AI エージェントへ提供され、又は前記ブラウザ操作連携機能へ転送される前に、所定の監査条件に基づいて記録、提示、秘匿化、要約化、又は拒否の少なくとも一つを行い、

前記監査記録部は、前記監査記録へのアクセスを、前記ブラウザ、前記一又は複数の AI エージェント又は前記 Gateway モジュールに対応する権限を有する主体に限定する、

ことを特徴とするシステム。

【請求項5】

請求項2に記載のシステムにおいて、

前記ブラウザ操作連携機能は、前記ブラウザが提供する制御機構、連携機構又は操作補助機構の少なくとも一つを用いて、前記共有許可済み対象に対する情報取得、表示制御、入力制御、外部資源指定、及び注釈レイヤ実行の少なくとも一つを実行する処理と、

前記ブラウザのアドオン機構を介さずに、前記ブラウザ又は前記ブラウザ相当環境の表示画面、ウィンドウ、タブ、又は操作対象要素と連携する外部操作連携部により、前記共有許可済み対象に対応する取得対象識別情報、表示対象識別情報、表示領域情報、又は位置情報の少なくとも一つに基づいて操作対象を特定し、前記 Gateway モジュールから受信した操作要求又は注釈要求を当該操作対象に限定して実行する処理と、

第1のブラウザ連携機構による実行が前記ブラウザの権限又は実行環境の制限により拒否された場合に、第2のブラウザ連携機構により前記共有許可済み対象に対する処理を実行する処理と、

の少なくとも一つを実行する、

ことを特徴とするシステム。

【請求項6】

請求項2に記載のシステムにおいて、

前記 Gateway モジュール又は前記 Gateway モジュールと連携するエージェント操作インターフェースが、ブラウザ内容又は前記共有許可済み対象に係る内容の情報取得、読み取り、注釈、操作、共有許可管理、操作履歴取得、及び反復操作支援の少なくとも一つを、前記一又は複数の AI エージェントが呼び出し可能な形式で提供する処理と、

前記 Gateway モジュールが、前記ブラウザ操作連携機能から受信したブラウザ内容又は前記共有許可済み対象に係る内容を、前記一又は複数の AI エージェントへ返送する前に、所定の変換処理により変換する処理と、

前記 Gateway モジュールが、対象条件及び変換処理指定情報を含む変換定義を読み込み、前記共有許可済み対象の取得対象識別情報が前記対象条件に合致する場合に、当該対象条件に対応する変換処理を実行する処理と、

の少なくとも一つを実行する、

ことを特徴とするシステム。

【請求項7】

請求項1に記載のシステムにおいて、

前記 Gateway モジュール又は前記 Gateway モジュールと連携するエージェント操作インターフェースは、前記ブラウザ若しくはブラウザ相当環境の表示画面又は操作対象に対する複数の操作を操作列として記録し、前記操作列を実行前にユーザーへ提示する実行前提示処理と、前記操作列を当該表示画面又は当該操作対象に対して再実行する再実行処理とを提供し、反復操作検証又は自動操作検証に用いられる操作列を生成及び再実行可能にする、

ことを特徴とするシステム。

【請求項8】

請求項2に記載のシステムにおいて、

前記ブラウザ操作連携機能は、前記共有許可済み対象上に注釈レイヤを描画し、

前記注釈レイヤは、ユーザーによる領域指定、ユーザーによるテキスト選択、及び前記一又は複数の AI エージェントからの注釈追加要求の少なくとも一つを注釈入力として受け付け、当該注釈入力に対応する識別表示及びコメントを前記共有許可済み対象上に視覚的に表示し、

前記ブラウザ操作連携機能は、前記注釈入力が領域指定である場合に、前記ブラウザ内の構造化要素に対応する対象要素を推定できるときは構造要素注釈として分類し、当該対象要素を推定できないときは視覚領域注釈として分類し、

前記注釈入力がテキスト選択である場合に、選択文字列及び当該選択文字列を再特定するための再特定情報を含むテキスト注釈として分類し、

前記構造要素注釈、前記視覚領域注釈、又は前記テキスト注釈を、位置情報、表示対象識別情報、コメント、作成時刻、対象要素情報、文字列情報又は表示属性情報の少なくとも一つを含む注釈データとして保持する、

ことを特徴とするシステム。

【請求項9】

ブラウザと、一又は複数の AI エージェントとを、ブラウザ操作連携機能及び Gateway モジュールを介して連携させる方法であって、

前記 Gateway モジュールは、前記ブラウザ操作連携機能と前記一又は複数の AI エージェントとの間で、要求の中継、対象確認、共有許可管理、変換、監査記録、又はこれらに相当する処理の少なくとも一つを行う仲介機能であり、

前記ブラウザ、前記一又は複数の AI エージェント、及び前記 Gateway モジュールは、同一の情報処理装置上に配置されてもよく、互いに通信可能な複数の情報処理装置上に分散配置されてもよく、前記 Gateway モジュールは、前記ブラウザ内、前記ブラウザのアドオン機構内、オペレーティングシステム内、AI エージェント実行環境内、ネイティブアプリケーション内、サーバー上、クラウド環境上、又はこれらの組み合わせとして実装されてもよく、

前記ブラウザ操作連携機能が、ユーザーによる特定のタブ、ウィンドウ、表示領域又は操作対象についての明示的な共有許可操作を受け付けるステップと、

前記ブラウザ操作連携機能が、前記特定のタブ、ウィンドウ、表示領域又は操作対象を識別する情報、取得対象識別情報、表示対象識別情報、及び許可時取得対象識別情報を含む共有許可情報を、前記 Gateway モジュールに対し、通信路又は同一実行環境内の呼出しを介して送信し、又は受け渡すステップと、

前記 Gateway モジュールが、特定の AI サービス、AI プロバイダ、AI モデル、エージェント実行環境、クライアントアプリケーション又はツールプロトコルに依存しない呼出し形式により、前記一又は複数の AI エージェントから操作要求又は注釈要求を受信するステップと、

前記 Gateway モジュールが、前記操作要求又は前記注釈要求の対象が共有許可済み対象であるかを判定し、前記対象が共有許可済み対象である場合に前記操作要求又は前記注釈要求を前記ブラウザ操作連携機能に転送するステップと、

前記ブラウザ操作連携機能が、前記操作要求に応じて前記対象に対する読み取り若しくは操作を実行し、又は前記注釈要求に応じて前記対象上に注釈レイヤを描画するステップと、

前記ブラウザ操作連携機能が、前記対象の現在の取得対象識別情報が前記許可時取得対象識別情報と対応しない状態になったこと、前記対象が閉じられたこと、終了したこと、若しくは利用不能となったこと、又はユーザーによる明示的な取消操作を検知した場合に、当該対象の共有許可を失効させるステップと、

を含むことを特徴とする方法。

【請求項10】

一又は複数のコンピュータに、請求項9に記載の方法を実行させることを特徴とするプログラム。
